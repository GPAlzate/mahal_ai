# React Native / Expo tooling (Metro, Babel plugins) assumes a flat node_modules.
# pnpm's default isolated layout breaks transitive resolution (e.g. @babel/types
# from react-native-worklets), so use a hoisted node-linker for this workspace.
node-linker=hoisted
